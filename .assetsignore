# Що не викладати як статичні файли Worker-а (синтаксис як у .gitignore)
.git
.gitignore
.assetsignore
worker.js
wrangler.jsonc
vercel.json
# Файл даних понад 25 MiB — ліміт Cloudflare на один ассет. Живе в R2 (див. wrangler.jsonc).
Build/*.data.br
.wrangler
